name: Verify production

on:
  workflow_run:
    workflows: ["Deploy to Ukraine.com.ua"]
    types: [completed]
  workflow_dispatch:

jobs:
  verify:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    env:
      REMOTE_HOST: uageek.ftp.tools
      REMOTE_USER: uageek
      REMOTE_PATH: /home/uageek/baukostenradar.de/www
    steps:
      - name: Configure SSH
        shell: bash
        env:
          REMOTE_KEY: ${{ secrets.REMOTE_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.ssh
          printf '%s\n' "$REMOTE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p 22 -H "$REMOTE_HOST" >> ~/.ssh/known_hosts

      - name: Verify remote deployment files
        shell: bash
        run: |
          set -euo pipefail
          ssh -p 22 "$REMOTE_USER@$REMOTE_HOST" "
            test -f '$REMOTE_PATH/server.js';
            test -f '$REMOTE_PATH/package.json';
            test -d '$REMOTE_PATH/.next';
          "

      - name: Verify deployed commit is expected or newer
        if: ${{ github.event_name == 'workflow_run' }}
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.event.workflow_run.head_sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl -sS --fail 'https://baukostenradar.de/api/version' -o /tmp/version.json
          deployed_sha=$(jq -r '.sha // empty' /tmp/version.json)
          echo "Expected SHA: $EXPECTED_SHA"
          echo "Deployed SHA: $deployed_sha"

          if [[ "$deployed_sha" == "$EXPECTED_SHA" ]]; then
            echo "Production is running the expected commit."
            exit 0
          fi

          compare=$(curl -sS --fail \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H 'Accept: application/vnd.github+json' \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/compare/$EXPECTED_SHA...$deployed_sha")
          compare_status=$(printf '%s' "$compare" | jq -r '.status // empty')
          echo "GitHub compare status: $compare_status"

          if [[ "$compare_status" == "ahead" ]]; then
            echo "Production has already advanced to a newer descendant commit."
            exit 0
          fi

          echo "Production is neither the expected commit nor a newer descendant." >&2
          exit 1

      - name: Verify version endpoint
        if: ${{ github.event_name == 'workflow_dispatch' }}
        shell: bash
        run: |
          set -euo pipefail
          curl -sS --fail 'https://baukostenradar.de/api/version' -o /tmp/version.json
          jq -e '.service == "BauKostenRadar" and (.sha | length > 7) and .sha != "development"' /tmp/version.json >/dev/null

      - name: Verify canonical production site
        shell: bash
        run: |
          set -euo pipefail
          success=0
          for i in 1 2 3 4 5 6; do
            code=$(curl -sS -o /tmp/home.html -w '%{http_code}' --connect-timeout 10 --max-time 20 'https://baukostenradar.de/' || true)
            echo "Attempt $i: HTTP $code"
            if [[ "$code" == "200" ]] && grep -qi 'BauKostenRadar' /tmp/home.html; then
              success=1
              break
            fi
            sleep 5
          done
          test "$success" = "1"
          grep -q 'GTM-WCPZ9WFT' /tmp/home.html
          grep -q 'vXOTcob1kFIZony7RPFaEkolb_RX3iMjOJkWlS-LH38' /tmp/home.html

      - name: Verify favicon
        shell: bash
        run: |
          set -euo pipefail
          grep -qi 'rel="icon"' /tmp/home.html
          grep -qi 'icon.svg' /tmp/home.html
          code=$(curl -sS -o /tmp/icon.svg -w '%{http_code}' --connect-timeout 10 --max-time 20 'https://baukostenradar.de/icon.svg')
          echo "Favicon HTTP: $code"
          test "$code" = "200"
          grep -q '<svg' /tmp/icon.svg

      - name: Verify retired lead routes stay removed
        shell: bash
        run: |
          set -euo pipefail
          api_code=$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 10 --max-time 20 'https://baukostenradar.de/api/project-request')
          page_code=$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 10 --max-time 20 'https://baukostenradar.de/angebot')
          echo "Retired API HTTP: $api_code"
          echo "Retired page HTTP: $page_code"
          test "$api_code" = "404"
          test "$page_code" = "404"

      - name: Verify www redirects to apex
        shell: bash
        run: |
          set -euo pipefail
          code=$(curl -sS -o /dev/null -w '%{http_code}' --connect-timeout 10 --max-time 20 'https://www.baukostenradar.de/')
          location=$(curl -sSI --connect-timeout 10 --max-time 20 'https://www.baukostenradar.de/' | tr -d '\r' | awk 'tolower($1)=="location:" {print $2}' | tail -1)
          echo "www HTTP: $code"
          echo "Location: $location"
          [[ "$code" == "308" || "$code" == "301" ]]
          [[ "$location" == "https://baukostenradar.de" || "$location" == "https://baukostenradar.de/" ]]

      - name: Verify legal pages are noindex
        shell: bash
        run: |
          set -euo pipefail
          curl -sS --fail 'https://baukostenradar.de/impressum' -o /tmp/impressum.html
          curl -sS --fail 'https://baukostenradar.de/datenschutz' -o /tmp/datenschutz.html
          grep -qi 'noindex' /tmp/impressum.html
          grep -qi 'noindex' /tmp/datenschutz.html

      - name: Verify robots blocks API routes
        shell: bash
        run: |
          set -euo pipefail
          curl -sS --fail 'https://baukostenradar.de/robots.txt' -o /tmp/robots.txt
          grep -q 'Disallow: /api/' /tmp/robots.txt

      - name: Verify sitemap excludes non-indexable pages
        shell: bash
        run: |
          set -euo pipefail
          curl -sS --fail 'https://baukostenradar.de/sitemap.xml' -o /tmp/sitemap.xml
          for path in '/impressum' '/datenschutz' '/angebot'; do
            if grep -q "$path" /tmp/sitemap.xml; then
              echo "$path must not be present in sitemap.xml" >&2
              exit 1
            fi
          done

      - name: Verify representative SEO templates
        shell: bash
        run: |
          set -euo pipefail

          curl -sS --fail 'https://baukostenradar.de/kosten' -o /tmp/costs.html
          curl -sS --fail 'https://baukostenradar.de/kosten/maler' -o /tmp/service.html
          curl -sS --fail 'https://baukostenradar.de/kosten/maler/leistung/waende-streichen' -o /tmp/item.html
          curl -sS --fail 'https://baukostenradar.de/staedte/berlin' -o /tmp/city.html
          curl -sS --fail 'https://baukostenradar.de/kosten/maler/berlin' -o /tmp/local.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber' -o /tmp/guides.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber/sanierungskosten-pro-qm' -o /tmp/guide.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber/dach-150-qm-kosten' -o /tmp/dach-scenario.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber/20-fenster-austauschen-kosten' -o /tmp/fenster-scenario.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber/bad-10-qm-sanieren-kosten' -o /tmp/bad-scenario.html
          curl -sS --fail 'https://baukostenradar.de/ratgeber/fussbodenheizung-100-qm-kosten' -o /tmp/heizung-scenario.html
          curl -sS --fail 'https://baukostenradar.de/rechner' -o /tmp/calculators.html
          curl -sS --fail 'https://baukostenradar.de/rechner/renovierungskosten' -o /tmp/renovation-calculator.html
          curl -sS --fail 'https://baukostenradar.de/rechner/handwerkerkosten' -o /tmp/trade-calculator.html
          curl -sS --fail 'https://baukostenradar.de/rechner/badsanierungskosten' -o /tmp/bath-calculator.html
          curl -sS --fail 'https://baukostenradar.de/rechner/handwerkerkosten?gewerk=maler&leistung=waende-streichen&region=berlin&menge=25' -o /tmp/deep-trade-calculator.html

          grep -q 'https://baukostenradar.de/kosten' /tmp/costs.html
          grep -q 'https://baukostenradar.de/kosten/maler' /tmp/service.html
          grep -q 'https://baukostenradar.de/kosten/maler/leistung/waende-streichen' /tmp/item.html
          grep -q 'https://baukostenradar.de/staedte/berlin' /tmp/city.html
          grep -q 'https://baukostenradar.de/kosten/maler/berlin' /tmp/local.html
          grep -q 'https://baukostenradar.de/ratgeber' /tmp/guides.html
          grep -q 'https://baukostenradar.de/ratgeber/sanierungskosten-pro-qm' /tmp/guide.html
          grep -q 'https://baukostenradar.de/rechner' /tmp/calculators.html
          grep -q 'https://baukostenradar.de/rechner/renovierungskosten' /tmp/renovation-calculator.html
          grep -q 'https://baukostenradar.de/rechner/handwerkerkosten' /tmp/trade-calculator.html
          grep -q 'https://baukostenradar.de/rechner/badsanierungskosten' /tmp/bath-calculator.html
          grep -q 'https://baukostenradar.de/rechner/handwerkerkosten' /tmp/deep-trade-calculator.html

          for page in /tmp/costs.html /tmp/service.html /tmp/item.html /tmp/city.html /tmp/local.html /tmp/guides.html /tmp/guide.html /tmp/dach-scenario.html /tmp/fenster-scenario.html /tmp/bad-scenario.html /tmp/heizung-scenario.html /tmp/calculators.html /tmp/renovation-calculator.html /tmp/trade-calculator.html /tmp/bath-calculator.html /tmp/deep-trade-calculator.html; do
            if grep -qi 'noindex' "$page"; then
              echo "Unexpected noindex in $page" >&2
              exit 1
            fi
          done

          grep -q 'BreadcrumbList' /tmp/costs.html
          grep -q 'ItemList' /tmp/costs.html
          grep -q 'BreadcrumbList' /tmp/item.html
          grep -q 'FAQPage' /tmp/item.html
          grep -q 'Beispielrechnungen' /tmp/item.html
          grep -q 'gewerk=maler' /tmp/item.html
          grep -q 'leistung=waende-streichen' /tmp/item.html
          grep -q 'BreadcrumbList' /tmp/city.html
          grep -q 'BreadcrumbList' /tmp/local.html
          grep -q 'gewerk=maler' /tmp/local.html
          grep -q 'leistung=waende-streichen' /tmp/local.html
          grep -q 'region=berlin' /tmp/local.html
          grep -q 'CollectionPage' /tmp/guides.html
          grep -q 'ItemList' /tmp/guides.html
          grep -q 'Article' /tmp/guide.html
          grep -q 'FAQPage' /tmp/guide.html
          grep -q 'Passende Gewerke und konkrete Preispositionen' /tmp/guide.html
          grep -q '/kosten/badsanierung/leistung/' /tmp/guide.html
          grep -q '/staedte/berlin' /tmp/guide.html

          grep -q 'gewerk=dachsanierung' /tmp/dach-scenario.html
          grep -q 'leistung=neueindeckung-inkl-lattung' /tmp/dach-scenario.html
          grep -q 'menge=150' /tmp/dach-scenario.html
          grep -q 'gewerk=fenster' /tmp/fenster-scenario.html
          grep -q 'leistung=fenster-mit-2-fach-verglasung' /tmp/fenster-scenario.html
          grep -q 'menge=20' /tmp/fenster-scenario.html
          grep -q 'gewerk=badsanierung' /tmp/bad-scenario.html
          grep -q 'leistung=bad-neubau-sanierung' /tmp/bad-scenario.html
          grep -q 'menge=10' /tmp/bad-scenario.html
          grep -q 'gewerk=heizung' /tmp/heizung-scenario.html
          grep -q 'leistung=fussbodenheizung-nachruesten' /tmp/heizung-scenario.html
          grep -q 'menge=100' /tmp/heizung-scenario.html

          grep -q 'CollectionPage' /tmp/calculators.html
          grep -q 'ItemList' /tmp/calculators.html
          grep -q 'WebApplication' /tmp/renovation-calculator.html
          grep -q 'WebApplication' /tmp/trade-calculator.html
          grep -q 'WebApplication' /tmp/bath-calculator.html
          grep -q 'Große Kostenblöcke separat prüfen' /tmp/renovation-calculator.html
          grep -q '/kosten/badsanierung/leistung/' /tmp/renovation-calculator.html
          grep -q 'Häufige Gewerke und konkrete Preispositionen' /tmp/trade-calculator.html
          grep -q '/kosten/maler/leistung/waende-streichen' /tmp/trade-calculator.html
          grep -q 'Die Preisanker des Rechners im Detail' /tmp/bath-calculator.html
          grep -q '/kosten/badsanierung/leistung/' /tmp/bath-calculator.html
          grep -q '/kosten/badsanierung/berlin' /tmp/bath-calculator.html
          grep -q 'Vorauswahl aus der vorherigen Kostenseite übernommen' /tmp/deep-trade-calculator.html
          grep -q 'Wände streichen' /tmp/deep-trade-calculator.html
          grep -q 'Berlin' /tmp/deep-trade-calculator.html
          grep -q 'Menge: 25' /tmp/deep-trade-calculator.html

          for path in \
            '/kosten' \
            '/kosten/maler' \
            '/kosten/maler/leistung/waende-streichen' \
            '/staedte/berlin' \
            '/kosten/maler/berlin' \
            '/ratgeber' \
            '/ratgeber/sanierungskosten-pro-qm' \
            '/ratgeber/dach-150-qm-kosten' \
            '/ratgeber/20-fenster-austauschen-kosten' \
            '/ratgeber/bad-10-qm-sanieren-kosten' \
            '/ratgeber/fussbodenheizung-100-qm-kosten' \
            '/rechner' \
            '/rechner/renovierungskosten' \
            '/rechner/handwerkerkosten' \
            '/rechner/badsanierungskosten'; do
            grep -q "$path" /tmp/sitemap.xml
          done
